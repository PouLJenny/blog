回答关于这个代码库的问题。只读代码,不要修改任何文件。

问题:运行 `python -m minilog.cli sample_logs/app.log --slow-only`(不带其他参数)时:

1. 一条日志行要出现在最终输出里,必须同时满足哪些条件?逐条列出,并注明每个条件由哪个文件的哪个函数实施。
2. 判定"慢"的阈值默认值是多少?定义在哪个文件?除了 --slow-only 的过滤路径之外,这个阈值还影响哪个输出行为?
3. sample_logs/app.log 里编号为 1002 和 1003 的两条 order 日志,会不会出现在上述命令的输出里?为什么?

用简洁的中文列表作答。
